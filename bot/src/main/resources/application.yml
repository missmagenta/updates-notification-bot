app:
  telegram-token: ${TELEGRAM_BOT_TOKEN}
  name: "@updates_notification_bot"
  scrapper-api-url:
    http://localhost:8080

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

springdoc:
  swagger-ui:
    path: "/swagger-ui"

server:
  port: 8090

logging:
  config: classpath:log4j2-plain.xml

