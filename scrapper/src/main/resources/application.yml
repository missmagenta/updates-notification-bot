app:
  scheduler:
    enable: true
    interval: 10s
    force-check-delay: 10s

spring:
  application:
    name: scrapper
  codec:
    max-in-memory-size: 500KB

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
